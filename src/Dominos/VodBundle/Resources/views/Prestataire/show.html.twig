{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Prestataire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nompresta</th>
                <td>{{ entity.nompresta }}</td>
            </tr>
            <tr>
                <th>Startpresta</th>
                <td>{{ entity.startpresta|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Endpresta</th>
                <td>{{ entity.endpresta|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('prestataire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('prestataire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
